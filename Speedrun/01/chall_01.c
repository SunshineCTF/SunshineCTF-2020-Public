#include <stdio.h>
#include <stdlib.h>

void main(void)
{

	char sample[20];
	char buf[50];
	int target0;
	int target1;

	puts("Long time ago, you called upon the tombstones");

	fgets(sample, sizeof(sample) - 1, stdin);

	gets(buf);

	if (target0 == 0xfacade)
	{
		system("/bin/sh");
	}

	if (target1 == 0xfacade)
	{
		system("/bin/sh");
	}
}

