#include <stdio.h>
#include <stdlib.h>

void main(void)
{

	char sample[20];
	char buf[50];
	int target0;
	int target1;

	puts("You can hear the sound of a thousand...");

	fgets(sample, sizeof(sample), stdin);

	gets(buf);

}

